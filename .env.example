# Required — generate with: openssl rand -base64 32
JWT_SECRET=

# Optional: force secure cookies when using HTTPS (even outside NODE_ENV=production)
# COOKIE_SECURE=true

# Optional: enable Helmet security headers in development
# HELMET=true

# Optional: disable Vite HMR (npm run dev) if the page keeps hot-reloading / flashing
# BERAMETHODE_NO_HMR=1

# Optional: return reset code in JSON + log to console (development only; never in production)
# ALLOW_RESET_DEV_CODE=true

# Google Gemini — server only (used by /api/ai/*). Never exposed to the browser bundle.
GEMINI_API_KEY=

# SMTP Configuration
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=465
SMTP_USER=no-reply@yourdomain.com
SMTP_PASS=your_password_here
SMTP_SECURE=true

# RH / paie — ajustement type SAGE (priorité sur app_settings : même clés côté serveur)
# HR_SAGE_ROUNDING=15
# HR_SAGE_WORKDAY_START=06:00
# HR_SAGE_APPLY=true

# Supabase auto-sync (PC → cloud → Vercel/mobile)
# Quand ces deux variables sont renseignées, le serveur Express pousse le
# snapshot SQLite vers Supabase user_data après chaque écriture, pour que la
# version Vercel (statique) voie les mêmes données pour le même e-mail.
# SUPABASE_OWNER_EMAIL=soulaimaneberraadi@gmail.com
# SUPABASE_OWNER_PASSWORD=
# Optional overrides (defaults baked into the code match vercel.json):
# SUPABASE_URL=https://utrojjhscyatppgcszrt.supabase.co
# SUPABASE_ANON_KEY=
# SUPABASE_SYNC_DEBOUNCE_MS=2000
