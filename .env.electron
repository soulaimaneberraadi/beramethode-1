# Build EXE Electron (mode 'electron') — local-first.
# Force le mode SERVEUR Express (Setup wizard + auth locale SQLite), PAS le mode
# statique Supabase (réservé à Vercel). Surcharge .env.local (priorité mode).
VITE_STATIC_MODE=false
VITE_DEV_AUTOLOGIN=
