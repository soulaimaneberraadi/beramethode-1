# خطة — إعادة تصميم النوافذ المنبثقة في صفحة Sous-traitance

> **اللغة:** موثّق بالعربية الفصحى، المصطلحات التقنية بالإنجليزية/الفرنسية كما هي في الكود.
> **الملف المعني:** `components/SousTraitance.tsx`

---

## 🎯 الهدف

تحسين تجربة إنشاء أمر جديد (New Order) في صفحة **Sous-traitance** عبر:
1. إزالة تأثير الـ `backdrop-blur` من جميع النوافذ المنبثقة (تسبب ازعاجاً بصرياً).
2. تدفّق على **خطوتين** عند الضغط على "Nouvelle Commande".
3. عرض معلومات الموديل عند اختياره في نموذج الأمر (صورة + اسم + تاريخ إنشاء).
4. عرض **ثمن الموديل** (المواد الخام فقط) في الأعلى، وتحته **ثمن الخياطة / الصنعة**.
5. اختيار المقاول (sous-traitant) وتحديد ثمن القطعة.
6. صفحة/نافذة مخصّصة لإضافة مقاول جديد (عوض الحقل النصي المباشر).

---

## 1️⃣ إزالة الـ blur من جميع النوافذ المنبثقة

### الملف: `components/SousTraitance.tsx`

النوافذ التي لا تزال تحمل `backdrop-blur-sm` (يجب إزالتها لتصبح متّسقة مع الباقي):

| السطر الحالي | النافذة | الوصف |
|---|---|---|
| `2339` | `isEditModalOpen` | تعديل أمر |
| `2738` | `isDetailModalOpen` | بطاقة تفاصيل الأمر |
| `2873` | `isSaleModalOpen` | فاتورة بيع |

### الإجراء
استبدال `bg-slate-900/55 dark:bg-black/70 backdrop-blur-sm` بـ `bg-slate-900/60 dark:bg-black/80` (نفس النمط المعتمد في Step 1 وStep 2، بدون blur).

---

## 2️⃣ Step 1 — نافذة الاختيار (موجودة، يجب التحقق منها)

عند الضغط على **"Nouvelle Commande"** → تفتح نافذة `isChoiceModalOpen` بخيارين:

| الخيار | السلوك |
|---|---|
| 📚 **Créer un nouveau modèle** | إغلاق النافذة + `onNavigate('library')` → مكتبة لإنشاء موديل جديد |
| 📦 **Choisir un modèle existant** | إغلاق النافذة + فتح Step 2 (`openOrderFormModal()`) |

**الزرّان الإضافيان:**
- زر **X** (إغلاق) في الأعلى.
- زر **"Retour à Sous-traitance"** في الأسفل (`setIsChoiceModalOpen(false)`).

> ✅ موجودة حالياً — تُراجَع بصرياً فقط.

---

## 3️⃣ Step 2 — نموذج الأمر الجديد (`isAddModalOpen`)

### عند اختيار الموديل (`selectedModelInForm`):
تعرض **بطاقة غنية** تحتوي:

1. **الصورة المصغّرة** (thumbnail) للموديل.
2. **اسم الموديل** + المرجع (Ref).
3. **تاريخ الإنشاء** (Date de création).
4. **ثمن الموديل** = المواد الخام فقط (`baseModelPrice`) — **بدون** ثمن الخياطة.
5. **تحته مباشرة**: **ثمن الخياطة / الصنعة** (`stitchingPrice`) — MAD/pc.

### ملاحظة تنظيمية
الترتيب الحالي معروض في شبكة من عمودين (`grid grid-cols-2`) متساوية الوزن.
**المطلوب:** إبراز ثمن الموديل أولاً، ثم ثمن الخياطة **أسفله** بتصميم متدرّج.

### اختيار المقاول (sous-traitant):
- قائمة منسدلة بقائمة المقاولين المستخرجين من الطلبيات (`subcontractorNames`).
- خيار **"+ Ajouter un nouveau sous-traitant"** → يفتح نافذة إضافة مقاول (انظر القسم 4).

---

## 4️⃣ نافذة مخصّصة لإضافة مقاول جديد

### الحالة الحالية
عند اختيار `__NEW__` يظهر حقل نصي inline فقط لكتابة الاسم.

### الحالة المطلوبة
نافذة منبثقة مخصّصة (`isNewSubModalOpen`) بعنوان **"Nouveau Sous-traitant / ورشة جديدة"** تحتوي على:

| الحقل | النوع |
|---|---|
| اسم الورشة / المقاول * | نص |
| الهاتف | نص (+212...) |
| التقييم (rating) | رقم 0–5 |
| تاريخ التوفر | تاريخ |

**عند الحفظ:**
- تُضاف الورشة إلى قائمة المقاولين (يُعاد حساب `subcontractorNames`).
- يُعبّأ `formSubcontractorName` و`formSubcontractorPhone` و`formSubcontractorRating` و`formSubcontractorAvailabilityDate`.
- تُغلق النافذة وتبقى نافذة الأمر مفتوحة.

---

## 📋 قائمة الملفات المتأثرة

| الملف | نوع التغيير |
|---|---|
| `components/SousTraitance.tsx` | كل التعديلات (blur + Step1/Step2 + نافذة مقاول) |

---

## ✅ فحص الجودة

1. `npx tsc --noEmit` → يجب أن يكون الخروج 0 أخطاء.
2. فحص بصري في المتصفح:
   - Nouvelle Commande → نافذة خيارين (بدون blur).
   - اختيار موديل → بطاقة بصورة + تاريخ + ثمن الموديل فوق + ثمن الخياطة تحته.
   - "+ Ajouter" → نافذة مخصّصة لإدخال بيانات الورشة.
